implemented with group by
inserted between group by and order by
using having with aggregate fuctions


select
first_name, count(first_name) as names_count

from 
employees

group by
first_name



Having count(first_name) >250

order by first_name;
